export const APP_SCHEME = "@app:company-web/";

export enum StorageKeys {
  Theme = `${APP_SCHEME}theme`,
  Locale = `${APP_SCHEME}locale`,
  User = `${APP_SCHEME}user`,
  AccessToken = `${APP_SCHEME}access_token`,
  EstablishmentPosts = `${APP_SCHEME}establishment_posts`,
  Establishment = `${APP_SCHEME}establishment`,
  EstablishmentPaymentMethods = `${APP_SCHEME}establishment_payment_methods`,
  EstablishmentFoodRestrictions = `${APP_SCHEME}establishment_food_restrictions`,
}
